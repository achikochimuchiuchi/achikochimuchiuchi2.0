{
  "entities": {
    "scandal": {
      "title": "Administrative/Civil Servant Scandal",
      "description": "A record of a scandal involving administrative bodies or individual civil servants.",
      "type": "object",
      "properties": {
        "title": { "type": "string", "description": "The title of the scandal news." },
        "date": { "type": "string", "description": "The date the scandal was reported." },
        "category": { "type": "string", "enum": ["Administrative", "Personal"], "description": "Whether the scandal is organizational or individual." },
        "description": { "type": "string", "description": "A brief summary of the scandal." },
        "sourceUrl": { "type": "string", "format": "uri", "description": "The URL of the news source." },
        "location": { "type": "string", "description": "The prefecture or city where the scandal occurred." },
        "createdAt": { "type": "string", "format": "date-time", "description": "The time the record was added to the database." }
      },
      "required": ["title", "date", "category", "description", "sourceUrl", "createdAt"]
    },
    "archiveItem": {
      "title": "Inaction Archive Item",
      "description": "A record of inaction, unspoken words, or unselected futures.",
      "type": "object",
      "properties": {
        "title": { "type": "string", "description": "The title of the inaction record." },
        "date": { "type": "string", "description": "The date of the record." },
        "category": { "type": "string", "description": "The category of inaction." },
        "description": { "type": "string", "description": "A detailed description of the inaction." },
        "intensity": { "type": "number", "description": "The intensity of the inaction (0-1)." },
        "createdAt": { "type": "string", "format": "date-time", "description": "The time the record was added to the database." }
      },
      "required": ["title", "date", "category", "description", "intensity", "createdAt"]
    }
  },
  "firestore": {
    "/scandals/{scandalId}": {
      "schema": "scandal",
      "description": "A collection of administrative and civil servant scandals collected from news sources."
    },
    "/archive/{archiveId}": {
      "schema": "archiveItem",
      "description": "A collection of user-submitted inaction records."
    }
  }
}
